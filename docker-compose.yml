version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: olimpika-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: olimpika
      POSTGRES_USER: olimpika_user
      POSTGRES_PASSWORD: olimpika_pass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:

